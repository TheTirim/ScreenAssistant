<Window x:Class="TabZeroAssistant.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TabZeroAssistant.Wpf.ViewModels"
        xmlns:res="clr-namespace:TabZeroAssistant.Wpf.Resources"
        mc:Ignorable="d"
        Title="{x:Static res:Strings.AppTitle}"
        Height="720" Width="900"
        xmlns:local="clr-namespace:TabZeroAssistant.Wpf">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#DDD" BorderThickness="1" Padding="8" CornerRadius="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding ChatMessages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:ChatMessageViewModel}">
                            <StackPanel Margin="0,4">
                                <TextBlock FontWeight="SemiBold" Text="{Binding Role}" />
                                <TextBlock Text="{Binding Content}" TextWrapping="Wrap" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,12">
            <TextBox Width="700" Height="32" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" />
            <Button Width="120" Margin="8,0,0,0" Content="{x:Static res:Strings.Send}" Click="OnSendClicked" />
        </StackPanel>

        <GroupBox Grid.Row="2" Header="{x:Static res:Strings.Suggestions}" Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Suggestions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:SuggestionViewModel}">
                            <Border Margin="0,0,0,8" Padding="8" BorderBrush="#E1E1E1" BorderThickness="1" CornerRadius="8">
                                <StackPanel>
                                    <TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding Title}" />
                                    <TextBlock Text="{Binding Reason}" TextWrapping="Wrap" Margin="0,4,0,8" />
                                    <ItemsControl ItemsSource="{Binding Actions}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Margin="0,0,4,0" Padding="8,4" Content="{Binding Type, Converter={StaticResource ActionLabelConverter}}" Click="OnActionClicked" Tag="{Binding}" />
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <Button Content="{x:Static res:Strings.FeedbackPositive}" Width="44" Click="OnFeedbackPositive" Tag="{Binding}" />
                                        <Button Content="{x:Static res:Strings.FeedbackNegative}" Width="44" Margin="8,0,0,0" Click="OnFeedbackNegative" Tag="{Binding}" />
                                        <Button Content="{x:Static res:Strings.FeedbackNever}" Margin="8,0,0,0" Click="OnFeedbackNever" Tag="{Binding}" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="{x:Static res:Strings.MemoryCandidates}" Padding="8" Margin="0,12,0,0" Visibility="{Binding MemoryCandidatesVisible}">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding MemoryCandidates}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:MemoryCandidateViewModel}">
                            <CheckBox Content="{Binding Candidate.Content}" IsChecked="{Binding Selected}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button Content="{x:Static res:Strings.ButtonSaveMemories}" Margin="0,8,0,0" Click="OnSaveMemories" />
            </StackPanel>
        </GroupBox>

        <TextBlock Grid.Row="4" Text="{Binding StatusMessage}" Foreground="DarkSlateGray" Margin="0,8,0,0" />
    </Grid>
</Window>
